generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model images {
  id        String   @id @default(cuid())
  messageId String
  url       String
  fileName  String
  fileSize  Int
  createdAt DateTime @default(now())
  messages  messages @relation(fields: [messageId], references: [id], onDelete: Cascade)

  @@index([messageId])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model messages {
  id        String   @id @default(cuid())
  sermonId  String
  content   String
  order     Int
  createdAt DateTime @default(now())
  images    images[]
  sermons   sermons  @relation(fields: [sermonId], references: [id], onDelete: Cascade)

  @@index([sermonId, order])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sermons {
  id        String     @id @default(cuid())
  title     String
  pastor    String
  date      DateTime   @default(now())
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  userId    String?
  user      User?      @relation(fields: [userId], references: [id], onDelete: Cascade)
  messages  messages[]

  @@index([userId])
}

// Tablas de la Biblia
model books {
  booknum   Int     @id
  bookname  String
  testament String?
  category  String?
  words     words[]
}

model words {
  wordid   Int     @id
  word     String?
  booknum  Int?
  chnum    Int?
  versenum Int?
  books    books?  @relation(fields: [booknum], references: [booknum], onDelete: NoAction, onUpdate: NoAction)
}

model bible_verses {
  id           Int     @id @default(autoincrement()) @db.SmallInt
  tema         String?
  libro        String?
  capitulo     BigInt?
  versiculo    String?
  codigo_libro String?
}

model favorites_verses {
  id          BigInt    @id @default(autoincrement())
  user_id     BigInt
  booknum     Int
  chapter     Int
  start_verse Int
  end_verse   Int
  text        String
  created_at  DateTime? @default(now()) @db.Timestamp(6)
}

// NextAuth.js Models
model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model User {
  id            String         @id @default(cuid())
  name          String?
  email         String         @unique
  emailVerified DateTime?
  image         String?
  password      String?
  accounts      Account[]
  sessions      Session[]
  sermons       sermons[]
  dailyReadings DailyReading[]
  diaryEntries  DiaryEntry[]
  notifications Notification[]
  pushSubscriptions PushSubscription[]
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

// Daily Bible Reading Tracking
model DailyReading {
  id        String   @id @default(cuid())
  userId    String
  date      DateTime @default(now()) @db.Date
  passage   String   // Requerido: e.g., "Juan 3:16-20"
  seeds     Int      @default(10) // Semillas de fe (puntos) por leer
  createdAt DateTime @default(now())
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, date])
  @@index([userId])
  @@index([date])
}

// Versículo del Día - un versículo fijo para cada día
model VerseOfTheDay {
  id          String   @id @default(cuid())
  date        DateTime @unique @db.Date // La fecha del versículo
  reference   String   // e.g., "Juan 3:16"
  text        String   @db.Text // Texto completo del versículo
  book        String   // Nombre del libro
  chapter     Int      // Número de capítulo
  verse       String   // Número(s) de versículo
  version     String   @default("simple") // Versión de la Biblia
  tema        String?  // Tema opcional
  createdAt   DateTime @default(now())
  
  @@index([date])
}

// Diario Personal - Entradas diarias del usuario
model DiaryEntry {
  id              String   @id @default(cuid())
  userId          String
  date            DateTime @default(now()) @db.Date // Fecha de la entrada
  gratitude       String?  @db.Text // 🕊 Agradece por algo
  thoughts        String?  @db.Text // 💭 ¿Qué estás pensando?
  mood            String?  // 😊 ¿Cómo te sientes hoy? (emoji o texto)
  moodScore       Int?     // Escala 1-5 opcional
  achievements    String?  @db.Text // ✅ ¿Qué lograste o aprendiste hoy?
  freeNote        String?  @db.Text // 🌙 Nota libre (opcional)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, date])
  @@index([userId])
  @@index([date])
}

// Sistema de Notificaciones
model Notification {
  id        String   @id @default(cuid())
  userId    String
  type      String   // "verse_of_day", "reading_reminder", "diary_reminder", "achievement", "custom"
  title     String
  message   String   @db.Text
  icon      String?  // emoji o url de icono
  link      String?  // URL a donde navegar al hacer click
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, read])
  @@index([createdAt])
}

// Suscripciones Push para notificaciones web
model PushSubscription {
  id         String   @id @default(cuid())
  userId     String
  endpoint   String   @unique
  p256dh     String
  auth       String
  createdAt  DateTime @default(now())
  
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}
